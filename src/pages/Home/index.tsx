import * as React from "react";
import Button from "../../components/Button";
import Input from "../../components/Input";

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <Button />
      <Input />
    </React.Fragment>
  );
};

export default Home;
